
[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "kurrent"
version = "0.1.0"

[project.scripts]
kurrent = "kurrent.cli:main"

[tool.setuptools.packages.find]
include = ["kurrent*"]
exclude = ["playground*", "test*", "tests*", "chroma_data*"]
